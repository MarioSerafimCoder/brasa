import { getProfilePreferences, saveProfilePreferences } from "../services/profile-service.js";
const DEVICE_KEY="brasa:device-preferences";
const deviceKeys=new Set(["theme","accent","density","posterStyle","libraryView"]);
export const defaultPreferences={theme:"dark",accent:"brasa",density:"normal",posterStyle:"poster-info",libraryView:"grid",skipIntro:true,skipCredits:true,subtitleLanguage:"pt-br",subtitleSize:"medium",subtitleFont:"Inter",subtitleColor:"#FFFFFF",subtitleBackground:"#000000",subtitleOpacity:70,subtitleOutline:true,subtitlePosition:"bottom",subtitleSync:0,audioLanguage:"original",maxVolume:100,normalizeVolume:false,preferSurround:true};
export function getPreferences(){let device={};try{device=JSON.parse(localStorage.getItem(DEVICE_KEY)||"{}");}catch{}return {...defaultPreferences,...device,...getProfilePreferences()};}
export function savePreferences(values){const device={},profile={};Object.entries(values).forEach(([key,value])=>(deviceKeys.has(key)?device:profile)[key]=value);if(Object.keys(device).length){let current={};try{current=JSON.parse(localStorage.getItem(DEVICE_KEY)||"{}");}catch{}localStorage.setItem(DEVICE_KEY,JSON.stringify({...current,...device}));}if(Object.keys(profile).length)saveProfilePreferences(profile);applyPreferences();document.dispatchEvent(new CustomEvent("brasa:preferences-change"));}
export function applyPreferences(){const p=getPreferences(),root=document.documentElement;root.dataset.theme=p.theme;root.dataset.accent=p.accent;root.dataset.density=p.density;root.dataset.posterStyle=p.posterStyle;root.dataset.libraryView=p.libraryView;root.dataset.subtitlePosition=p.subtitlePosition;root.dataset.subtitleOutline=p.subtitleOutline?"on":"off";root.style.setProperty("--user-subtitle-color",p.subtitleColor);root.style.setProperty("--user-subtitle-bg",p.subtitleBackground);root.style.setProperty("--user-subtitle-bg-rgb",hexToRgb(p.subtitleBackground));root.style.setProperty("--user-subtitle-opacity",String(Number(p.subtitleOpacity)/100));return p;}
function hexToRgb(value){const n=String(value||"#000000").replace("#","").padEnd(6,"0").slice(0,6),number=parseInt(n.length===3?n.split("").map((c)=>c+c).join(""):n,16);return Number.isFinite(number)?[(number>>16)&255,(number>>8)&255,number&255].join(","):"0,0,0";}
