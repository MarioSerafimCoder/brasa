const content=document.querySelector("#content");let loading=false,lastSignature="";
async function renderAndroidTvUpdate(){if(location.hash.split("/")[0]!=="#network"||!content||content.querySelector("#androidTvUpdate")||loading)return;loading=true;try{const response=await fetch("/api/admin/android-tv-update",{credentials:"same-origin",headers:{Accept:"application/json"}});if(!response.ok)return;const envelope=await response.json(),data=envelope.data;if(!data)return;const update=data.update,signature=JSON.stringify(data);if(signature===lastSignature&&content.querySelector("#androidTvUpdate"))return;lastSignature=signature;const devices=(data.devices||[]).map((device)=>`<tr><td>${escapeHtml(device.name)}</td><td>${escapeHtml(device.clientVersionName||"Não informada")}</td><td>${device.clientVersionCode??"—"}</td><td>${device.lastUpdateCheckAt?new Date(device.lastUpdateCheckAt).toLocaleString("pt-BR"):"Nunca"}</td><td>${device.revoked?"Revogado":"Ativo"}</td></tr>`).join("");content.insertAdjacentHTML("beforeend",`<section id="androidTvUpdate" class="panel" style="margin-top:16px"><h2>Atualização do BRasa TV</h2>${update?`<div class="grid-2"><div><p><strong>Versão ${escapeHtml(update.versionName)} (${update.versionCode})</strong></p><p>${formatBytes(update.sizeBytes)} · ${new Date(update.publishedAt).toLocaleString("pt-BR")}</p><p>SHA-256: <code>${escapeHtml(update.sha256.slice(0,16))}…</code></p><p>Certificado: <code>${escapeHtml(update.signingCertificateSha256.slice(0,16))}…</code></p></div><div><p><strong>Notas</strong></p><ul>${(update.releaseNotes||[]).map((note)=>`<li>${escapeHtml(note)}</li>`).join("")||"<li>Sem notas.</li>"}</ul></div></div>`:'<div class="empty">Nenhuma atualização foi publicada localmente.</div>'}<p class="muted">${data.androidTvDevices||0} dispositivo(s) Android TV conhecido(s). A publicação é feita somente pelo script local; upload pelo navegador não está disponível.</p>${devices?`<div class="table-wrap"><table><thead><tr><th>Dispositivo</th><th>Versão</th><th>versionCode</th><th>Última verificação</th><th>Status</th></tr></thead><tbody>${devices}</tbody></table></div>`:""}</section>`);}catch{}finally{loading=false;}}
function escapeHtml(value){return String(value??"").replace(/[&<>'"]/g,(character)=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[character]);}function formatBytes(value){return new Intl.NumberFormat("pt-BR",{style:"unit",unit:"megabyte",maximumFractionDigits:1}).format(Number(value||0)/1048576);}
new MutationObserver(renderAndroidTvUpdate).observe(content,{childList:true,subtree:false});window.addEventListener("hashchange",()=>setTimeout(renderAndroidTvUpdate));renderAndroidTvUpdate();
