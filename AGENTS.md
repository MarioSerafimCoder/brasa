# BRasa workspace instructions

## Android TV release signing

- Never ask the user to paste APK signing passwords into chat.
- When signing input is required, launch `ASSINAR ATUALIZACAO BRASA TV.vbs` through the Windows window manager (`explorer.exe`) so the form appears in the foreground.
- Do not use a hidden terminal or a background-only password prompt for this interaction.
